#An example to trigger actions on a subset of timesteps

- 
  action: "add_triggers"
  triggers: 
    t1: 
      params: 
        condition: "cycle() % 50 == 0"
        actions_file: "trigger_ascent_actions.yaml"
#       actions_file: "trigger_isocontours_actions.yaml"
